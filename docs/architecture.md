# Architecture

## Product intent

Pilldue helps you track medications and know when each one needs a refill. You log therapy schedules; the app surfaces refill timing.

## Current decisions

| Topic | Status |
|-------|--------|
| UI | Undecided — console TUI vs WPF |
| Application project | Not scaffolded yet |
| Source location | Planned under `src/` |

## Planned structure

Once a UI approach is chosen, application code will live under `src/`. Architecture details (layers, data storage, domain model) will be recorded here as they are decided.
